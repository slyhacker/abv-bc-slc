describe("getPrime", function() {
  it(" it should generates prime numbers from 0 to n , function() {
    expect(getPrime(0)).toBe(true);("even intergers can not be a prime);
});

it("for every number between 2 to N, check whether it is prime number or not", function()
expect(getPrime)(2))).toBe(true);("2 is a prime number");
});

it("should return an empty array if argument is less than 2", function() {
 expect(getPrimes(1)).toBe("1 is not a prime number");
      });

it("should return ([2, 3]) if argument is less than 2", function() {
        expect(getPrimes([2, 3])).toBe(true);([2,3] are prime numbers")
    
it("should return error if intergers numbers are negative", function() {
	expect(getPrime([-1  to -100])).toBe(true);99[-1 to -100] are not prime numbers

it("should determine 17 is a prime number", function() {
    expect(isPrime(17)).toBeTruthy();
  });
  
  it("should determine 18 is not a prime number", function() {
    expect(isPrime(18)).toBeFalsy();
  });
  
  it("should determine 19 is a prime number", function() {
    expect(isPrime(19)).toBeTruthy();
  });
  
  it("should determine 20 is not a prime number", function() {
    expect(isPrime(20)).toBeFalsy();

   it("should determine 37,120,123 is a prime number", function() {
    expect(isPrime(37120123)).toBeTruthy();
  });
});