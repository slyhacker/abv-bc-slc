function reverseString(str) {
	if (str === "")
		return "";
	
function palindrome(str) {
	return str ===st.split('').reverse().join('');
}

palindrome('anna')
true

palindrome('NaN')
true

palindrome('civic')

true

const reverse = (str) => {
	for (let i= str.length; i<=str.length; i--){
		return reverse}

		console.assert(reverse('books') === 'skoob'
		console.assert(reverse('solomon') === 'nomolos'
		console.assert(reverse)('misc') === 'csim'	
	}
});

	









